PixelNote Palace — Fixed MVP
This is a minimal, fixed MVP with React + Vite + react-konva + pdfjs-dist + Dexie.
Run:
  cd pixelnote-palace-fixed
  npm install
  npm run dev
Open http://localhost:5173